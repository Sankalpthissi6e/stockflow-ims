# StockFlow IMS — Production Environment
# Using /api (relative path) because vercel.json proxies /api/* to the Render backend.
# This avoids CORS issues entirely since all requests stay on the same domain.
VITE_API_URL=/api
